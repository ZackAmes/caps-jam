{
  "dependencies": {},
  "devDependencies": {
    "@smthrs/pi-plugin": "^0.35.0"
  },
  "name": "caps",
  "private": true,
  "workspaces": [
    "client",
    "bot",
    "packages/*"
  ],
  "scripts": {
    "test": "bun test client/tests bot/tests",
    "check": "bun run --cwd client check && bun run --cwd bot check",
    "bot": "bun run --cwd bot src/main.ts"
  }
}
